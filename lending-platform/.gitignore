# ── Node / npm ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp
.pnp.js

# ── Environment variables (NEVER commit secrets) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Build outputs ──
dist/
build/
.vite/

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/
.idea/
*.swp
*.swo

# ── Hardhat / Blockchain ──
cache/
artifacts/
typechain/
typechain-types/

# ── Temporary scripts (created during setup) ──
create-app.ps1
frontend/init-frontend.ps1
frontend_readme_backup.md
